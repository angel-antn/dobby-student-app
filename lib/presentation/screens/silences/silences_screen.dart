import 'package:flutter/material.dart';

class SilencesScreen extends StatelessWidget {
  const SilencesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('SilencesScreen'),
      ),
    );
  }
}
